import './index.scss';
import React from 'react';

const Nav = () => {
  return(
    <nav className = "nav__container">
      <div className = "nav__logo" to = "/"> Toy Robot </div>
    </nav>
  );};

export default Nav;